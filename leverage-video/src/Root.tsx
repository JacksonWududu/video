import React from 'react';

// @ts-ignore -- episode roots are intentionally local, gitignored workspaces.
import {Root as Topic2Root} from './topic2/script/video/Root';

export const Root: React.FC = () => <Topic2Root />;
