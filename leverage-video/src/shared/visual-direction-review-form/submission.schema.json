{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "visual-direction-form-submission-v1",
  "title": "Visual Direction Form Submission v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "contract_version",
    "episode_workspace",
    "presented_map_sha256",
    "submission_scope",
    "rows"
  ],
  "properties": {
    "contract_version": {"const": "visual-direction-form-submission-v1"},
    "episode_workspace": {"type": "string", "minLength": 1},
    "presented_map_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "submission_scope": {
      "type": "object",
      "additionalProperties": false,
      "required": ["mode", "shot_ids"],
      "properties": {
        "mode": {"enum": ["all", "selected"]},
        "shot_ids": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {"type": "string", "pattern": "^S[0-9]{2,}$"}
        }
      }
    },
    "rows": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "shot_id",
          "white_cat_present",
          "visual_generation_route",
          "visible_text_mode",
          "exact_visible_text",
          "visible_text_placement"
        ],
        "properties": {
          "shot_id": {"type": "string", "pattern": "^S[0-9]{2,}$"},
          "white_cat_present": {"type": "boolean"},
          "visual_generation_route": {
            "enum": [
              "imagegen",
              "xuan-paper-diorama",
              "ian-handdrawn-ppt",
              "ink-doodle-knowledge-card",
              "srt-whiteboard-animation"
            ]
          },
          "visible_text_mode": {"enum": ["none", "required"]},
          "exact_visible_text": {"type": ["string", "null"]},
          "visible_text_placement": {"type": ["string", "null"]}
        }
      }
    }
  }
}
