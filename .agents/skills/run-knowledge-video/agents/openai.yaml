interface:
  display_name: "Run Knowledge Video"
  short_description: "Run density-aware workflows with v3 visual and semantic transitions"
  default_prompt: "Use $run-knowledge-video to collect style, conditional scope, density, approval mode, and narration source in one atomic post-cover selection batch; preserve every later human gate, exact hash binding, final visual-list approval, caption-delivery choice, and revoice visual lock."
