interface:
  display_name: "Run Knowledge Video"
  short_description: "Run videos with v3 visual and semantic transitions"
  default_prompt: "Use $run-knowledge-video to create or resume a knowledge video, collect checksum-bound v3 route/text choices plus storyboard-visual-rhythm motion tiers, use stateful 2–4 or hero_pose 4–6 semantic assets where justified, default adjacent v3 images to direct cuts and require explicit approval for watercolor, process generated visuals before deferred local video, reject retired routes, require the final caption choice, or retime an eligible episode while preserving state identity, assets, and every effect."
