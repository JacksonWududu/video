interface:
  display_name: "Assemble Video Master"
  short_description: "Assemble approved visuals through route-specific scenes"
  default_prompt: "Use $assemble-video-master to validate the current approved v3 visual-direction map, storyboard-visual-rhythm, action-state-schedule-v3, and exact intra-shot-transition-v1 maps, while accepting legacy read-only input only under its unchanged historical contract; render cuts or explicitly approved watercolor through IntraShotImageSequence without fallback while preserving unchanged v2 legacy behavior; route active visuals through their shared scenes; reject retired output routes; ask for the final caption-delivery choice and deliver only selected full-video roles."
