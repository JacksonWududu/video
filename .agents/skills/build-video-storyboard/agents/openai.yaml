interface:
  display_name: "Build Video Storyboard"
  short_description: "Build storyboards with v3 visual and semantic transitions"
  default_prompt: "Use $build-video-storyboard to build an audio-timed storyboard with explicit standard/rich density, lock storyboard-visual-rhythm-v2 and action-state-schedule-v4 hashes/counts/fallbacks, preserve standard counts, enforce rich bounds and clean-frame/UTF-8 rules, derive concise visible-text candidates and require one whole-map user approval in both manual and one-click modes, reject retired routes, or retime a locked sequence without changing density, states, assets, or effects."
