interface:
  display_name: "Produce Video Visuals"
  short_description: "Produce approved narrative and graphic visuals"
  default_prompt: "Use $produce-video-visuals to consume the approved v3 direction and storyboard-visual-rhythm maps, generate and approve layered/stateful/hero_pose assets at their locked counts, preserve each exact intra-shot-transition-v1 entry with cut as default and watercolor only when explicitly approved, process generated/Whiteboard items before deferred local-video sources, reject retired routes, and review every exact output sequentially."
