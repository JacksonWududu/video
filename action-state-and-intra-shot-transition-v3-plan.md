# 动作状态与镜内转场 v3 优化方案

## 总体方案

- 新增 `action-state-schedule-v3`：图片数由语义动作节点决定，时长公式仅作参考。
- 新增 `motion_tier`：`layered` 为 1 张主图加 3–10 个局部分层；`stateful` 为 2–4 张完整状态图，默认 2–3 张；`hero_pose` 为 1 张固定背景加 4–6 张透明姿态图，通常 4–5 张。
- `stateful` 第 4 张须对应第四个独立旁白语义和非重复结果，并填写 `state_count_rationale`。超过 5 个连续状态只允许出现在 6 姿态 `hero_pose` 中，先评估拆镜；无法自然拆分才取得绑定镜头与方案哈希的专项批准。
- `75 帧`改为节奏提示；任一状态均可保持至下一个真实语义节点，长保持须记录理由。
- 取消 v3 多图分镜强制 `watercolor-bloom`：相邻图片默认使用 0 帧硬切，可按需单独选择水彩效果。
- 既有 v2、topic4、GEN-THINK 及当前未提交改动保持原样，不批量迁移。

## 契约与公共接口

新增 `storyboard-visual-rhythm-v1`：

- 每镜锁定 `motion_tier`、注意力功能、视觉问题与回报、有效变化事件、视觉结构、资产/复用计划、动作表演计划、镜头运动复杂度，以及身份/动作/道具/空间/光线/视线连续性。
- 高风险边界须写退出状态、进入状态、不变量、允许变化和剪辑动机。
- `medium_high_v1` 以 4 秒有效变化、相同结构最多连续两次、18 秒显著构图变化、前 30 秒至少三种结构和 `hero_pose` 约 10%–25% 作为 QA 警告，不作为机械增图依据。转场本身不计为有效变化。
- 该工件使用独立 `presented_map_sha256`，与七列视觉方向表在同一次审核中批准，不增加门禁；镜内转场种类属于其哈希投影，可选水彩另需绑定该哈希的明确批准。

在共享动作调度模块中：

- 保留 `buildActionStateSchedule` 作为 v2 接口；新增 `buildActionStateScheduleV3`、`retimeActionStateScheduleV3`。
- `validateActionStateSchedule` 按 `contract_version` 分派；类型声明改为 v2/v3 联合类型。
- v3 输入包含：
  - `total_frames`、`fps: 30`；
  - 有序状态的 `state_id`、非空 `semantic_state`、镜内 `at_frame`；
  - 每个状态对应的锁稿原文 UTF-8 字节区间及精确文本；
  - 超过 75 帧状态的 `semantic_hold_reason`；
  - `motion_tier`、第四张 stateful 理由，以及 6 姿态 hero family 的不可拆说明与审批证据。
- v3 不再平均分配帧数；状态起点来自真实音频对齐，当前图片保持至下一个状态起点或镜头结束。
- 每个状态须有至少 15 帧清晰展示时间；其最小时长为“所选进入转场帧数 + 15 帧”。
- 原 `round(F/45)` 结果仅写入 `cadence_advisory`，不得决定图片数或导致拆镜。
- 6 姿态 `hero_pose` 必须具有：
  - `split_assessment.natural_semantic_pause_available = false`；
  - 非空不可拆理由；
  - `extended_family_approval`，包含用户原话、时间、状态、视觉方向映射 SHA-256 与镜头状态方案 SHA-256。

新增共享 `intra-shot-transition-v1`：

- 每个相邻图片边界仍须有一条明确记录，数量恒为 `N-1`，但不再强制可见动画。
- 初始目录仅注册：
  - `cut`：默认，0 帧，无动画尾部；
  - `watercolor-bloom`：可选，0.6 秒、18 帧，复用现有渲染器。
- 禁止 `none`、未知效果、隐式替换及未注册效果。
- v3 builder 未收到特别选择时，自动生成完整的 `N-1` 条 `cut` 记录。
- `cut` 后状态最少持续 15 帧；`watercolor-bloom` 后状态最少持续 33 帧。
- 保留现有 `WatercolorImageSequence` API；新增通用 `IntraShotImageSequence`，依每条边界决定直接切换或调用水彩渲染器。

## 审核与工作流接入

- 分镜阶段先按 `layered | stateful | hero_pose` 锁定资产结构，再列不可合并的动作、反应或结果并绑定真实音频帧；不得为凑数量或满足 75 帧制造姿势。
- 七列视觉方向审核表不增列；在角色镜头下附“动作状态与镜内切换方案”，展示：
  - 状态顺序、触发原文和切换帧；
  - 每段保持时长及长保持理由；
  - 每对图片的 `cut` 或可选 `watercolor-bloom`；
  - 建议数量、实际数量及不可拆说明。
- 完整状态与镜内切换方案纳入 `presented_map_sha256`。
- 默认硬切可随完整推荐映射批准；选择水彩效果须绑定当前映射。6 姿态 hero family 仍须对该镜头单独明确批准，`确认全部推荐`不得代替扩展审批。
- 状态数、顺序、切换帧、长保持理由或镜内效果变化时，重开该镜头视觉方向审核并使其动作族素材与预览失效。
- 仅首态、末态或镜头时间改变时，重开相邻镜间 `scene-transition-v3`；普通内部切换变化不影响镜间转场。
- 视觉生产继续逐图生成和审核；动作连续性、角色一致性、精确字节审批及状态顺序不变。
- 装配计划不得自行重建水彩链；v3 直接消费已批准的镜内切换记录，并验证图片顺序、边界索引、持续时间与渲染器。
- `NarrativeScene`、`GraphicScene` 等 v3 消费方改用通用序列组件；v2 和 GEN-THINK 继续走原水彩组件，输出像素与证据不变。
- 将通用镜内切换模块登记进共享复用注册表，并补真实消费者、校验和及导入标记证据。
- 更新 build、produce、assemble 与 orchestrator 的相关说明；不新增全局关卡，故不修改 `AGENTS.md`。

## 兼容与迁移

- v2 继续执行原五张硬上限、平均分配及强制 `N-1` 水彩转场。
- 已完成或未重开的 v2 分镜、topic4、GEN-THINK、现有水彩组件及审批证据均保持可读、可验证、可复现。
- 不自动改写任何 episode。既有动作族仅在用户明确重新规划该镜头时，创建版本化 v3 清单。
- v2 revoice 维持原规则；v3 revoice 根据各状态绑定的原文区间重新定位切换帧，并锁定状态 ID、顺序、图片字节及镜内效果。
- 若 v3 revoice 后某个状态不足“转场帧数 + 15 帧”，则阻断派生，不静默改成硬切或其他效果。

## 测试与验收

- v2 回归：
  - 375 帧五态通过，376 帧仍按旧规则失败；
  - topic4、S18、GEN-THINK、水彩组件及现有装配测试保持通过。
- v3 数量：
  - `stateful` 使用 1 张或 5 张失败，2、3、4 张通过；4 张缺理由、语义重复或文本映射不完整时失败；
  - `hero_pose` 使用 4–6 张姿态；6 张须在时间可行、无自然拆分点且审批有效时通过；
  - 6 个以上连续状态或缺审批、哈希过期、存在合理拆分点、泛化授权时失败。
- v3 默认切换：
  - 多图未特别选择时生成完整 `N-1` 条 `cut`；
  - 相邻帧严格连续，无动画尾部、重叠或空帧；
  - 状态少于 15 帧时失败。
- v3 可选水彩：
  - 单条或混合边界可选择 `watercolor-bloom`；
  - 水彩边界必须为 18 帧，目标状态至少保留 15 帧清晰画面；
  - 缺记录、索引错误、未知效果、`none` 或错误渲染器时失败。
- 长保持：
  - 中间态及终态超过 75 帧、有语义理由时通过；
  - 缺理由时失败，不得自动新增状态。
- 集成：
  - 状态清单、视觉生产队列、图片顺序、镜内切换、装配计划和 Remotion 输出完全一致；
  - `cut` 验证相邻前后帧，水彩验证首帧、中间帧与完成帧。
- 验收标准：
  - 新分镜不再由时长公式机械决定图片数；
  - 不再强制任何 v3 多图镜头使用水彩转场；
  - 默认硬切无额外时长；
  - 水彩仍可按需选用；
  - 既有 v2 文件、审批及渲染结果不变。
